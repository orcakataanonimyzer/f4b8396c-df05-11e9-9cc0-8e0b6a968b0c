/*
const write = require("./writeFaster");
let pencil = { point: 10000000 };
let paper = { content: "" };
let text = "";
let textA = "";
let textB = "";
while (text.length < 10000000) {
  text += " ";
  textA += "a";
  // textB += "hello ";
}

describe("write()", () => {
  test("", () => {
    write(pencil, paper, textA);
    expect(pencil.point).toBe(0);
    expect(paper.content).toBe(textA);
  });
  // test("", () => {
  //   write(pencil, paper, text);
  //   expect(pencil.point).toBe(10000000);
  //   expect(paper.content).toBe(text);
  // });
  // test("", () => {
  //   write(pencil, paper, textB);
  //   expect(paper.content).toBe(textB);
  // });
});
*/
